{
  "config": {
    "step": {
      "user": {
        "title": "Connect Thessla Green gateway",
        "description": "Enter the local FastAPI gateway address. Home Assistant does not access Modbus directly.",
        "data": {
          "url": "Gateway URL",
          "token": "API token (optional)"
        }
      },
      "confirm": {
        "title": "Thessla Green unit discovered",
        "description": "Model: {model}\nFirmware: {firmware}\nSerial number: {serial_number}\nGateway Modbus owner endpoint: {endpoint}\nModbus unit ID: {unit_id}\nSerial ports visible on the gateway host:\n{serial_ports}\n\nThe gateway remains the sole bus owner. HACS reads snapshots through its API and never opens Modbus directly.",
        "data": {}
      },
      "reauth": {
        "title": "Re-authenticate gateway",
        "data": {
          "token": "API token"
        }
      },
      "reconfigure": {
        "title": "Change Thessla Green gateway",
        "data": {
          "url": "Gateway URL",
          "token": "API token (optional)"
        }
      }
    },
    "error": {
      "invalid_url": "Enter a valid HTTP or HTTPS URL.",
      "cannot_connect": "Unable to connect to the gateway or the device is offline.",
      "invalid_auth": "The gateway rejected the API token.",
      "device_not_found": "The gateway responds, but no confirmed AirPack unit is available. Run gateway discovery and configuration first."
    },
    "abort": {
      "already_configured": "This device is already configured.",
      "reauth_successful": "Re-authentication was successful.",
      "reconfigure_successful": "Gateway configuration was updated.",
      "unknown_error": "Unknown configuration error."
    }
  }
}
